version: '3.8'

services:
  lab:
    build: .
    volumes:
      - .:/app
    environment:
      - TERM=xterm-256color
    stdin_open: true # Permite interação (essencial para IPython) 
    tty: true
    command: ipython